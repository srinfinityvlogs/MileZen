// This is a placeholder. Once your Supabase project is created and you've
// run the schema SQL, generate real types with:
//
//   npx supabase login
//   npx supabase gen types typescript --project-id YOUR_PROJECT_REF --schema public > types/database.ts
//
// Real generated types give you compile-time safety on every table/column
// name — worth doing before writing much more app code.
export type Database = any;
